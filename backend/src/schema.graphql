# import * from './generated/prisma.graphql'

type Mutation {
    createItem(
        title: String,
        escription: String,
        price: Int,
        image: String,
        largeImage: String
    ): Item!
}

type Query {
    items: [Item]!
}